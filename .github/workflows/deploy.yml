name: "Déploiement Terraform"

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environnement à déployer'
        required: true
        default: 'test'
        type: choice
        options:
          - test
          - nuuk
          - prod
      action:
        description: 'Action Terraform à exécuter'
        required: true
        default: 'plan'
        type: choice
        options:
          - plan
          - apply

jobs:
  terraform:
    name: "Terraform Execution"
    runs-on: self-hosted
    defaults:
      run:
        working-directory: ./terraform
    
    env:
      TF_TOKEN_app_terraform_io: ${{ secrets.TF_API_TOKEN }}
      TF_VAR_proxmox_api_url: ${{ secrets.PVE_API_URL }}
      TF_VAR_proxmox_api_token_id: ${{ secrets.PVE_TOKEN_ID }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Display Configuration
        if: github.event_name == 'workflow_dispatch'
        run: |
          echo "🔧 Configuration du déploiement:"
          echo "  • Environnement: ${{ github.event.inputs.environment }}"
          echo "  • Action: ${{ github.event.inputs.action }}"

      - name: Terraform Init
        run: terraform init

      - name: Terraform Format
        run: terraform fmt -check
        continue-on-error: true

      - name: Terraform Plan (Manual Dispatch)
        if: github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'plan'
        run: terraform plan -var-file="environnements/${{ github.event.inputs.environment }}/terraform.tfvars"

      - name: Terraform Plan (Push/PR)
        if: github.event_name != 'workflow_dispatch'
        run: terraform plan -var-file="environnements/test/terraform.tfvars"
        continue-on-error: true

      - name: Terraform Apply
        if: github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'apply'
        run: terraform apply -var-file="environnements/${{ github.event.inputs.environment }}/terraform.tfvars" -auto-approve