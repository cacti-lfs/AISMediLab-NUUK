name: "Test Manual Run"

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment"
        required: true
        default: "test"
        type: choice
        options:
          - test
          - nuuk
          - prod
      action:
        description: "Action"
        required: true
        default: "plan"
        type: choice
        options:
          - plan
          - apply

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Environment: ${{ github.event.inputs.environment }}"
      - run: echo "Action: ${{ github.event.inputs.action }}"
      - run: echo "✅ Workflow fonctionne!"
